name: 2 - workflow trigger event
on: 
    push:
    pull_request:
    workflow_dispatch:
    schedule:
        - cron: '*/5 * * * *'
jobs:
    echo:
        runs-on: ubuntu-latest
        steps: 
            - name: show the trigger
              run: echo "I have been triggered by a(n) ${{ github.event_name }} event."