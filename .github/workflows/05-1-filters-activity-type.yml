name: 05 - 01 - event filters and activity type
on: 
    pull_request:
        types:
            - opened
            - synchronize
        branches:
            - main

jobs:
    echo:
        runs-on: ubuntu-latest
        steps:
            - run: echo "running whenever a PR is opened or syncronized and base branch is main"
              